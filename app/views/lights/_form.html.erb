<%= form_with(model: light, local: true) do |form| %>
  <% if light.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(light.errors.count, "error") %> prohibited this light from being saved:</h2>

      <ul>
      <% light.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
